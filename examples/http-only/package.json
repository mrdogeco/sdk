{
  "name": "@mrdoge-examples/http-only",
  "version": "0.0.0",
  "private": true,
  "description": "HTTP-only Mr. Doge SDK example. Runs anywhere fetch exists — Node cron, Lambda, Cloudflare Workers, Vercel Edge.",
  "type": "module",
  "scripts": {
    "start": "tsx index.ts"
  },
  "dependencies": {
    "@mrdoge/http": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
